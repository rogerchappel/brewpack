# sample-tool fixture

A deterministic fixture used by brewpack tests and smoke runs. Its release
artifact URL and `dist/tea-time` install path represent the contents expected in
the uploaded `tea-time-1.2.3.tar.gz` archive.
